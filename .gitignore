.DS_Store
node_modules
.next
out
dist
build
.env
.env*.local
*.log
coverage

# TypeScript incremental build cache
tsconfig.tsbuildinfo

# Per-machine Claude settings and worktrees
.claude/settings.local.json
.claude/worktrees/
CLAUDE.local.md

# Scratch space (resume PDF etc.) — never publish to a public repo
temp/
